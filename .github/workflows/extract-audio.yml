name: Extract YouTube Audio
on:
  repository_dispatch:
    types: [extract-audio]

jobs:
  extract:
    runs-on: ubuntu-latest
    steps:
      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'
          
      - name: Create test audio file
        env:
          VIDEO_TITLE: ${{ github.event.client_payload.title }}
          VIDEO_URL: ${{ github.event.client_payload.video_url }}
        run: |
          echo "Creating test audio file for: $VIDEO_TITLE"
          echo "Original video: $VIDEO_URL"
          
          sudo apt-get update
          sudo apt-get install -y ffmpeg
          
          ffmpeg -f lavfi -i "sine=frequency=440:duration=10" -acodec libmp3lame audio.mp3
          
          ls -la audio.mp3
          echo "Test audio file created successfully"
          
      - name: Upload to file storage
        id: upload
        run: |
          echo "Uploading test audio file..."
          
          # Check file exists
          ls -la audio.mp3
          echo "File size: $(du -h audio.mp3)"
          
          # Try 0x0.st (more reliable for automated uploads)
          echo "Uploading to 0x0.st..."
          DOWNLOAD_URL=$(curl -s -F "file=@audio.mp3" https://0x0.st)
          
          echo "Response: $DOWNLOAD_URL"
          
          # Check if we got a valid URL back
          if [[ $DOWNLOAD_URL == https://0x0.st/* ]]; then
            echo "download_url=$DOWNLOAD_URL" >> $GITHUB_OUTPUT
            echo "✅ Upload successful: $DOWNLOAD_URL"
          else
            echo "❌ Upload failed. Response was: $DOWNLOAD_URL"
            # For testing, let's use a dummy URL so the webhook still gets called
            DOWNLOAD_URL="https://example.com/test-audio.mp3"
            echo "download_url=$DOWNLOAD_URL" >> $GITHUB_OUTPUT
            echo "🔧 Using dummy URL for testing: $DOWNLOAD_URL"
          fi
          
      - name: Trigger n8n webhook
        env:
          N8N_WEBHOOK: ${{ secrets.N8N_WEBHOOK_URL }}
          TITLE: ${{ github.event.client_payload.title }}
          DESCRIPTION: ${{ github.event.client_payload.description }}
          VIDEO_URL: ${{ github.event.client_payload.video_url }}
          AUDIO_URL: ${{ steps.upload.outputs.download_url }}
        run: |
          echo "Calling n8n webhook..."
          echo "Audio URL: $AUDIO_URL"
          
          curl -s -X POST "$N8N_WEBHOOK" \
            -H "Content-Type: application/json" \
            -d "{\"title\":\"$TITLE\",\"description\":\"$DESCRIPTION\",\"video_url\":\"$VIDEO_URL\",\"audio_url\":\"$AUDIO_URL\"}"
          
          echo "Webhook called successfully"
